// TimeDiagramPage.tsx
import React from 'react';
import { Box, Typography } from '@mui/material';

const TimeDiagramPage: React.FC = () => {
  return (
    <Box sx={{ padding: 4 }}>
      <Typography variant="h4">Time Diagram Page Placeholder. Override everything if merge cocnflict arise once the actual page is done</Typography>
    </Box>
  );
};

export default TimeDiagramPage;